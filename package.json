{
  "name": "staratlas-meetups",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "anchor": "cd anchor && anchor",
    "anchor-build": "cd anchor && anchor build && cp target/deploy/meetups.so tests/fixtures/.",
    "anchor-test": "cd anchor && anchor test --skip-deploy --skip-local-validator"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.30.1",
    "@solana/web3.js": "^1.98.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/jest": "^29.5.14",
    "anchor-bankrun": "^0.5.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}