{
  "name": "staratlas-meetups",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "anchor": "cd anchor && anchor",
    "anchor-build": "cd anchor && anchor build && cp target/deploy/meetups.so specs/fixtures/.",
    "anchor-format": "cd anchor && cargo fmt",
    "anchor-specs": "cd anchor && anchor test --skip-deploy --skip-local-validator",
    "anchor-test": "cd anchor && anchor build && cargo test"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.30.1",
    "@solana/spl-token": "^0.4.12",
    "@solana/web3.js": "^1.98.0"
  },
  "devDependencies": {
    "@solana-developers/helpers": "^2.7.0",
    "@types/bun": "latest",
    "@types/jest": "^29.5.14",
    "anchor-bankrun": "^0.5.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}